[alias]
# Stricter than CI (`cargo clippy --all-targets -- -D warnings`): adds the
# pedantic group as warnings. Deliberate exceptions are local `#[allow]`s
# with a reason, next to the code they excuse.
lint = "clippy -- -W clippy::all -W clippy::pedantic"
