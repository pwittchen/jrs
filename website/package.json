{
  "name": "jrs-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --port 3000 ./index.html",
    "build": "rm -rf dist && bun build ./index.html --outdir dist --minify && cp install.sh dist/",
    "preview": "bun run build && bun ./preview.ts"
  },
  "devDependencies": {
    "@fontsource-variable/martian-mono": "^5",
    "@fontsource/ibm-plex-mono": "^5",
    "@types/bun": "latest"
  }
}
