# Check text out with LF on every platform. The test fixtures are compared
# byte for byte and the fixture repository's POMs are checksummed, so a CRLF
# checkout on Windows breaks both.
* text=auto eol=lf
